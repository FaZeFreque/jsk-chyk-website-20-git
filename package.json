{
  "name": "chyk-website",
  "version": "1.0.0",
  "private": true,
  "description": "CHYK static website + local content manager. The site itself needs no build step.",
  "scripts": {
    "build": "node tools/make-dist.js",
    "manager": "node content-manager/server.js",
    "check": "node tools/check-content.js",
    "dist": "node tools/make-dist.js"
  },
  "optionalDependencies": {
    "ffmpeg-static": "^5.3.0"
  }
}
